import React from 'react';

const home = (props) => {

    /*
    *
    * Returns nothing for an empty component for the home screen.
    * 
    */
    return (
        <React.Fragment>

        </React.Fragment>
    );
};

export default home;